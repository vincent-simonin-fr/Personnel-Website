'use client'

type ServicesProps = object

const Services = ({}: ServicesProps) => {
  return <div className='flex h-[calc(100vh-60px)] flex-col items-center justify-center'>Services</div>
}

export default Services
