'use client'

type AboutProps = object

const About = ({}: AboutProps) => {
  return (
    <div className='flex h-[calc(100vh-60px)] flex-col items-center justify-center'>
      Calendly WIP{/* <ScheduleCallLink /> */}
    </div>
  )
}

export default About
